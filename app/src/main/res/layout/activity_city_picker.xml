<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.addressselector.CityPickerActivity">

    <LinearLayout
        android:id="@+id/address_layout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_50"
        android:background="#fff"
        android:elevation="3dp"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/pic_contact_back"
            android:layout_width="@dimen/dp_50"
            android:layout_height="@dimen/dp_50"
            android:padding="@dimen/dp_18"
            android:src="@drawable/back" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:text="城市选择"
            android:textColor="#000"
            android:textSize="@dimen/sp_18" />
    </LinearLayout>

    <me.yokeyword.indexablerv.IndexableLayout
        android:id="@+id/indexableLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/address_layout"
        android:background="@android:color/white"
        app:indexBar_layout_width="@dimen/dp_40"
        app:indexBar_selectedTextColor="#999"
        app:indexBar_textColor="#000"
        app:indexBar_textSize="@dimen/sp_14"
        app:indexBar_textSpace="@dimen/dp_5" />

</RelativeLayout>